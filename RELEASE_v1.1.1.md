# Animius Web v1.1.1

重点更新：**AI 双语字幕可靠性修复** + **移动端适配**。

## 修复

- 🩹 **AI 双语字幕：过期/回源失败也能加载已缓存的翻译**
  - 之前字幕签名过期时接口直接报错，磁盘里翻好的缓存根本没查；现在**缓存优先**，回源失败自动回退旧缓存
- 🩹 **不再把半成品字幕当成品**
  - 之前翻译每批就把半成品写进正式缓存，进度接口误报"完成"，导致只翻一半就上屏、中断后永不续翻
  - 现在进度只写 `.part.json` 快照，**全部翻完才落地成品**；中断后可**续翻**，已翻批次不重复消耗 LLM 额度
- 🩹 字幕加载失败不再把坏地址丢给播放器（之前失败也照样 `switch`，字幕区直接空白）
- 🩹 AI 翻译轮询：60s 无进展自动超时提示（之前 `idle` 状态无限死等）；切换轨道/关闭开关自动取消旧轮询
- 🩹 同一集翻译任务去重，重复点击不再并发触发多遍翻译
- 🩹 播放器去掉 `min-height`，小屏不再把 16:9 画面撑变形

## 移动端适配（首次）

- 📱 顶栏压成**单行**：logo 缩小、导航横向滑动（隐藏滚动条）、隐藏用户名，适配刘海屏 safe-area —— 之前换行堆成一大块
- 📱 响应式布局：卡片墙 / 详情页（小封面+简介并排）/ 选集 / 登录 / 设置页在手机上自适应
- 📱 「← 返回详情」改为**内容区左下角**的普通按钮，不再悬浮遮挡播放器

## 升级

```bash
# 替换 jar 后重启即可（数据目录与字幕缓存兼容）
java -jar animius-server-v1.1.1.jar --port 8090 --data /opt/animius/data
```

> 提醒：v1.1.0 及更早版本可能留下**只翻了一半的字幕缓存**（旧 bug 产物，会被当完整品用）。升级后如发现某集中文只有一半，在设置页「清空双语字幕缓存」再重开 AI 双语即可重新翻完整。
