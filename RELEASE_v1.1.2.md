# Animius Web v1.1.2

修复 **AI 字幕串集** 的紧急 bug。

## 修复

- 🩹 **字幕/翻译不再串集**：缓存键之前只按轨道名（`label|lang`）生成，**每一集都相同** —— 看第二集会直接命中第一集的翻译缓存。现在缓存键包含集标识（`label|lang|集URL`），跨重新解析稳定、但**按集区分**
- 🩹 **原文 VTT 也缓存了**（`.orig`）：字幕签名过期后，原文和翻译都能继续从缓存加载/继续翻译 —— 之前取消勾选重选轨道会一直"字幕地址过期"
- 🩹 轨道选择器改用 `label|lang` 匹配：之前签名刷新后 URL 变化导致下拉框选不中轨道（表现为重选无效/报过期）
- 🩹 翻译任务优先使用已缓存原文，签名过期也能继续翻

## 升级

```bash
# 替换 jar 后重启即可
java -jar animius-server-v1.1.2.jar --port 8090 --data /opt/animius/data
```

> 缓存键已变更：旧缓存文件不再被读取（v1.1.1 及以前的缓存本身也有串集问题），建议升级后在设置页「清空双语字幕缓存」一次，重新生成。
